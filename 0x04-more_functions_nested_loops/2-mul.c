#include "main.h"

/**
 * mul - multiplies two integers
 * @x: The first number to multiply
 * @y: The second number to multiplies with first number
 * Return: x * y as result
 */

int mul(int x, int y)
{
	return (x * y);
}
