#include "main.h"
/**
 * add - add up two  integers
 * Description: to add two integers
 * @a: the first integer
 * @b: the second integer
 * Return: (0) success
 */

int add(int a, int b)
{
	int result = a + b;

	return (result);
}
